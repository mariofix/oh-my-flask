[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "{{ name }}"
version = "0.1.0"
description = "A Flask app scaffolded by omf."
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0",
]

[tool.hatch.build.targets.wheel]
packages = ["{{ name }}"]
